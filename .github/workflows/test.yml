name: Test Project
on: push
jobs:
    first-job:
        runs-on: ubuntu-latest
        steps:
            - name: Get the code
              uses: actions/checkout@v5
            - name: Install dependencies
              run: npm ci
            - name: Run tests
              run: npm test
    second-job:
        needs: first-job
        runs-on: ubuntu-latest
        steps:
            - name: Get the code
              uses: actions/checkout@v5
            - name: Install dependencies
              run: npm ci
            - name: Build visitors
              run:  npm run build
            - name: Deploy
              run: echo "Deploying!"

